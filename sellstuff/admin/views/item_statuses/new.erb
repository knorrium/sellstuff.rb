<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:item_statuses, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:item_statuses, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:item_status) %></h2>
    <div class="inner">
      <% form_for :item_status, url(:item_statuses, :create), :class => :form do |f| %>
        <%= partial "item_statuses/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
