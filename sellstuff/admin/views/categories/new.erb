<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:categories, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:categories, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:category) %></h2>
    <div class="inner">
      <% form_for :category, url(:categories, :create), :class => :form do |f| %>
        <%= partial "categories/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
